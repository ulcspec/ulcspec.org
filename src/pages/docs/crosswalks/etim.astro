---
// /spec/crosswalks/etim, placeholder. The four crosswalks (GLDF, ETIM,
// IES LM-63, EULUMDAT) are status="planned" at v1 per the crosswalks
// index page. PR-1b wires the upstream MDX once the mapping documents
// land in ulcspec/ULC (mappings/<standard>-crosswalk.md).

import Callout from '../../../components/Callout.astro';
import PageShell from '../../../components/PageShell.astro';
import SpecDocsLayout from '../../../components/SpecDocsLayout.astro';
---

<PageShell
  title="ETIM crosswalk - ULC specification"
  description="Field-by-field crosswalk between ULC and ETIM, the ElectroTechnical Information Model for product attributes in electrotechnical wholesale. Documentation in progress; the upstream MDX wires up in PR-1b."
>
  <SpecDocsLayout>
    <h1 class="page-h1">ETIM crosswalk.</h1>

    <Callout intent="default" label="Planned (documentation in progress)">
      <p>
        This crosswalk is planned at v1. The upstream mapping document
        will land at <code>mappings/etim-crosswalk.md</code> in
        <a href="https://github.com/ulcspec/ULC">ulcspec/ULC</a>; PR-1b
        wires it onto this route via the spec-sync Content Loader.
      </p>
      <p>
        For now, see the <a href="/docs/crosswalks">crosswalks overview</a>
        for the four planned crosswalks and their context.
      </p>
    </Callout>
  </SpecDocsLayout>
</PageShell>
